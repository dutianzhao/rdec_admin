<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.2//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="UtilMapper">
	<select id="city_by_parent_id" resultType="com.roland.model.City" parameterType="int">
		select * from t_city as t where t.parent_id = #{parentId}
	</select>
	
	<select id="status_by_type_id" resultType="com.roland.model.Status" parameterType="int">
		select t.`name` as typeName,t.id as typeId,s.* from rdec_status as s RIGHT JOIN rdec_type as t on s.type = t.id where t.id = #{typeId}
	</select>
	
	<select id="selCity" resultType="map">
		select * from t_city as c where c.id = #{cityId};
	</select>
	
</mapper>


 
























